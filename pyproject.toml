[tool.poetry]
name = "nada-algebra"
version = "0.1.1"
description = ""
authors = ["José Cabrero-Holgueras <jose.cabrero@nillion.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^1.26.4"
nada-dsl = "^0.2.1"
py-nillion-client = "^0.2.1"


[tool.poetry.group.dev.dependencies]
pytest = "^8.2.0"
black = "^24.4.2"

[tool.poetry.dev-scripts]
install_version = "scripts.install_version:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
